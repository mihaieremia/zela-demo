name: Deploy procedure to Zela

# Manual one-shot deploy of a single procedure picked from the dropdown.
# For automatic deploys on push, see deploy-on-push.yml.

on:
  workflow_dispatch:
    inputs:
      procedure:
        description: "Which procedure to deploy"
        required: true
        type: choice
        options:
          - hello_world
          - block_time
          - priority_fees
          - subscribe

jobs:
  deploy:
    uses: mihaieremia/zela-deployment/.github/workflows/build-and-deploy.yml@v1
    with:
      procedure: ${{ inputs.procedure }}
      project:   ${{ vars.ZELA_PROJECT }}
    secrets:
      key-id:     ${{ vars.ZELA_KEY_ID }}
      key-secret: ${{ secrets.ZELA_KEY_SECRET }}
